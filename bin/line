#!/usr/bin/env python

import sys
target_line  = int(sys.argv[1])
current_line = 0

for line in sys.stdin:
    current_line += 1
    if current_line == target_line:
       sys.stdout.write(line)
       exit(0)

exit(1)
